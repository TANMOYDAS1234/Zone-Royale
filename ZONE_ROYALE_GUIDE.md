# 🎮 Zone Royale — Complete Guide (Workflow + Codebase)

A lightweight, addictive top-down **battle royale** built with Flutter + Flame.
Runs on any Android phone, single codebase, offline single-player (fair bots) with
a multiplayer server prototype included.

---

## Changelog (latest first)

- **Full premium UI reskin** — every screen (Home, Missions/ARMORY, Shop, Profile/OPERATOR, Match Summary) matches the Stitch mockups: shared tactical header (crosshair logo + rank + coins), a unified bottom nav (HOME/SHOP/MISSIONS/PROFILE), animated boot splash, and the code-drawn crosshair logo. Character previews use the **real 2D operator** art (honest — no fake 3D renders).
- **Fix: INTERNET permission** was missing from the release manifest, so multiplayer failed with "could not connect" on release APKs. Added to `android/app/src/main/AndroidManifest.xml`.
- **Multiplayer terminal** — connect screen defaults to the live Render server (`wss://zone-royale.onrender.com`); ARMORY/FACTION/INTEL tabs route to Shop/Profile/Missions.
- **Cold-start resilience** — connecting now wakes a sleeping free-tier host (HTTP pre-warm) and retries the socket up to 5× with a *"Waking the server…"* state, so multiplayer no longer fails on the first connect after the server sleeps. Guide documents a free keep-alive pinger.
- **Pushed to GitHub** — full project at `github.com/TANMOYDAS1234/Zone-Royale` (ready for the Render Blueprint deploy).
- **Customise control placement (BGMI-style)** — Profile → *Customise Control Placement* opens a drag editor; all six touch controls — move/aim sticks, skill, grenade, reload and the fire-mode toggle — float at your saved positions in-match (persisted). On touch they leave the info row and float freely; on desktop they stay in the row.
- **Online multiplayer client** — MULTIPLAYER button → connect by server address + room code, live twin-stick arena rendering real players/bullets from server snapshots (`lib/net/`).
- **Custom rooms** — server groups players by a shared room code (empty = `PUBLIC`); empty rooms auto-close.
- **Render.com deploy** — `Dockerfile` + `render.yaml` for one-click free hosting; app connects via `wss://`.
- **Sound fix** — SFX now ship as real `.wav` assets (`assets/sfx/`, generated by `tool/gen_sfx.dart`) played via `AssetSource`; replaces the flaky in-memory byte path that was silent on some devices.
- Manual **tap-to-reload** (weapon panel) + reload progress bar + low-ammo warning.
- **Share result screenshot** on the end screen (`share_plus`) — BGMI/Free-Fire style.
- Draggable **control customization** — profile stores per-control positions (drag editor WIP).
- Ranked **difficulty curve** (scales with level, plateaus ~lvl 40) + **wider zoom**.
- Combat feedback: hit marker, floating damage numbers, kill feed, gas/low-HP vignettes, touch aim-assist.
- Realism: shell casings, muzzle smoke, footstep dust, blood pools, wall debris.
- Real **audio** (procedural via audioplayers) after a long native-build fight (R8 off fixed it).
- 5 heroes + skills + evolution, distinct hero gear, per-weapon tracers, themed maps, shop, missions, progression.
- Multiplayer **server prototype** (`server/`).
- Bug-hunt fixes: false-win, stale-HUD-reset, NaN safeNorm, mission decode, zone banner.

---

## 1. What it is

Top-down last-one-standing shooter: drop in, loot weapons, survive the shrinking
gas zone, get kills, win. Built for fast sessions and long-term retention.

**Live features (all shipped, on-device):**

- **Core BR:** movement, shooting ballistics, knockback, loot (weapons/medkits/grenades), shrinking gas zone, last-one-standing win.
- **Modes:** Skirmish (10) · Clash (25) · Warzone (50) — map size + zone timing scale to player count.
- **Maps:** 4 themes (Urban buildings, Forest trees, Compound rooms, Badlands boulders) — random or pickable, each with its own ground/cover look.
- **Weapons (9):** Pistol, Magnum, SMG, Shotgun, Marksman/DMR, Rifle, LMG, Minigun, Sniper — each with a distinct model + tracer style. Single/Auto fire toggle. **Grenades** (throw + AoE). Manual **tap-to-reload**.
- **Heroes (5):** STRIKER (Dash), BASTION (Shield), VORTEX (Frenzy), MERCY (Field Kit heal), BOOMER (Resupply) — each with a unique skill, **evolution/Top Form**, and distinct on-body gear. Bots are randomly assigned hero looks.
- **Progression:** XP + endless levels, 8 ranks (Recruit→Legend), coins, post-match rewards, **ranked difficulty** (bots get tougher with your level, capped ~lvl 40 so it stays winnable — BGMI/Free-Fire style).
- **Live-service:** daily missions (claim coins/XP), a coin **shop** (skins/accessories/guns/heroes + evolutions), persistent lifetime stats.
- **Customization:** name, 18 outfit colors, 5 skin tones, 10 accessories, starting weapon, hero, fire mode, controller (size/opacity/left-handed).
- **Feel & readability:** wide POV, minimap enemy radar, directional damage arcs, hit marker, floating damage numbers, kill feed, low-HP + gas vignettes, muzzle smoke, shell casings, footstep dust, blood pools, screen shake, camera lead, **touch aim-assist**, and **procedural audio** (gunshots/explosions/hits) + haptics.
- **UX:** home buttons (in-match + end screen), share result.

---

## 2. Tech stack

| Piece       | Choice                                         | Why                                                                    |
| ----------- | ---------------------------------------------- | ---------------------------------------------------------------------- |
| Framework   | **Flutter** (Dart)                       | one codebase, all devices                                              |
| Game engine | **Flame** (`flame`)                    | 2D game loop, canvas rendering                                         |
| Persistence | **shared_preferences**                   | profile/progression/shop save                                          |
| Audio       | **audioplayers** + `.wav` assets       | reliable`AssetSource` playback (WAVs baked by `tool/gen_sfx.dart`) |
| Share       | **share_plus** + **path_provider** | share the result screenshot to any app                                 |
| Multiplayer | **dart:io WebSocket** (client + server)  | pure Dart, no deps; custom rooms                                       |

**Almost no asset files** — all art is drawn with `Canvas`/`CustomPainter`; the
only assets are ~10 tiny synthesised `.wav` SFX (`assets/sfx/`, a few KB each,
regenerated any time with `dart run tool/gen_sfx.dart`). That's why the APK is
tiny (~20 MB).

---

## 3. Run it (development)

```bash
# from the project root: d:\Viral Game
flutter pub get
flutter devices                       # find your phone's id
flutter run -d <device-id>            # hot-reload dev build
```

Controls — **Phone:** left stick move · right stick aim & fire · 💣 grenade ·
tap the weapon panel to reload. **Desktop:** WASD move · mouse aim · click fire ·
R reload · G grenade · B fire mode · F skill.

---

## 4. Build & install a release APK

```bash
flutter build apk --release --target-platform android-arm64
flutter install -d <device-id>
# output: build\app\outputs\flutter-apk\app-release.apk  (~20 MB)
```

`--target-platform android-arm64` builds only for real ARM phones (fast, small).

---

## 5. The stable build config (important)

This project was built on a Windows machine whose antivirus + cross-drive setup
(`C:` pub cache, `D:` project) fought the Android toolchain. Three settings make
builds reliable — keep them unless you move to a clean CI box:

- **`android/gradle.properties`** → `kotlin.incremental=false`
  (fixes Kotlin "this and base files have different roots" cross-drive crash).
- **`android/app/build.gradle.kts`** → release buildType `isMinifyEnabled = false`,
  `isShrinkResources = false`
  (the antivirus was quarantining the R8-optimized `classes.dex` mid-build; this
  also made `audioplayers` link its resources cleanly).
- **`ndk { abiFilters += "arm64-v8a" }`** — single ABI, no wasted native builds.

**For a size-optimized Play Store `.aab`:** re-enable R8 (`isMinifyEnabled = true`,
`isShrinkResources = true`) and build all ABIs — do it on a machine/CI where the
antivirus won't touch the build folder, or after adding Windows Defender folder
exclusions for `D:\Viral Game`, `C:\PubCache`, and `C:\Users\<you>\.gradle`.

---

## 6. Codebase map

```
lib/
  main.dart              App entry, screens router, keyboard/mouse input, Sfx.init
  game/
    royale_game.dart     THE game (FlameGame): setup, bots, combat, zone, render, feedback
    entities.dart        Character, Bullet, Grenade, Loot, Particle, Obstacle
    config.dart          All tunables: weapons, heroes, modes, maps, palettes, customization
    char_art.dart        drawOperator() — character/gun/accessory/hero-gear rendering
    profile.dart         Profile singleton — persistence, progression, missions, shop
    sfx.dart             Audio: loads assets/sfx/*.wav via AssetSource + haptics
    mathx.dart           Vector helpers (angleOf, fromAngle, safeNorm, collision)
  net/
    net_client.dart      NetClient — WebSocket connect, send input, parse snapshots
    net_arena.dart       MultiplayerScreen — connect form + live networked arena
  ui/
    game_ui.dart         HUD, floating touch controls, minimap, overlays, ControlsEditor (drag layout)
tool/
  gen_sfx.dart           Bakes the procedural SFX to assets/sfx/*.wav
assets/sfx/              10 tiny generated .wav sound effects
server/
  bin/server.dart        Authoritative WebSocket server (movement/bullets/hits + rooms)
  Dockerfile             Render.com deploy image
  README.md              Run + LAN test + Render deploy walkthrough
render.yaml              One-click Render Blueprint (repo root)
android/
  app/build.gradle.kts   arm64-only, R8 off (build stability)
  gradle.properties      kotlin.incremental=false
pubspec.yaml             deps: flame, shared_preferences, audioplayers, share_plus, path_provider
```

**Where to change things:**

- Weapon stats / new gun → `config.dart` (`kWeapons`, `WeaponId`) + draw in `char_art.dart` (`_drawWeapon`) + `_preferred`/`_tracerW` in `royale_game.dart`.
- New hero/skill → `config.dart` (`kHeroes`, `SkillType`) + `char_art.dart` (`_drawHeroGear`) + `activateSkill`/effects in `royale_game.dart`.
- New map theme → `config.dart` (`kMapThemes`) + `_drawObstacles`/`_drawGround` in `royale_game.dart`.
- Balance (bot difficulty, zone, XP) → `royale_game.dart` (`_buildCharacters`, `_driveBot`, `_updateZone`) + `config.dart` (`kZonePhases`, `recordResult`).

---

## 7. Data & persistence (`profile.dart`)

A single `Profile.instance` saved via shared_preferences. Keys: name, outfit,
skin, accessory, hero, startWeapon, fireAuto, matchMode, mapChoice, stick
settings, level, xp, coins, matches, wins, kills, bestPlacement, missions (daily,
encoded strings), missionDay, owned (shop item ids like `o3`,`a5`,`w7`,`h2`,`e2`).
Everything is bounds-checked and decode-safe.

---

## 8. Multiplayer — LIVE (client + server)

Online play works end-to-end: an **authoritative server** (`server/`) runs the
one true simulation and a **client** (`lib/net/`) renders it.

### Client (`lib/net/`)

- `net_client.dart` — `NetClient`: connects over a `dart:io` WebSocket (no extra
  dependency; works on Android/iOS/desktop), sends `{type:input,...}` at ~30 Hz,
  parses `{type:state,...}` snapshots, exposes `players`/`bullets`/`me` and a
  `rev` `ValueNotifier` that bumps on each snapshot.
- `net_arena.dart` — `MultiplayerScreen`: a connect form (server address + room
  code + name), then the live twin-stick arena. Renders every real player with
  the same `drawOperator` art as single-player, plus bullets, HP bars, names,
  a "YOU" ring, an ALIVE/KILLS HUD, and an ELIMINATED banner.
- Entry point: the **MULTIPLAYER** button on the start menu
  (`Navigator.push(MultiplayerScreen())`).

### Server (`server/bin/server.dart`)

Pure `dart:io`. Movement + bullets + hits + kills at 20 Hz, broadcast to all
clients. **Custom rooms:** clients that send the same `room` code share a match
(empty code → `PUBLIC`); rooms auto-close when empty.

**Run it (free LAN play):**

```bash
cd server && dart pub get && dart run bin/server.dart   # listens on :8080
```

Find your PC's IPv4 (`ipconfig`, e.g. `192.168.1.5`), then on a phone on the
same Wi‑Fi open MULTIPLAYER and enter `ws://192.168.1.5:8080`. Allow Dart
through the Windows firewall if prompted.

**Message protocol (JSON):**

- Client → server: `{"type":"join","name":"Ava","room":"NIGHT"}`, then
  `{"type":"input","mx":..,"my":..,"aim":..,"fire":true}`.
- Server → client: `{"type":"welcome","id":N,"world":3200}` then 20 Hz
  `{"type":"state","players":[{id,x,y,aim,hp,alive,kills,name}],"bullets":[{x,y}]}`.

### Deploy to the internet — Render.com (free)

Ships with `server/Dockerfile` + `render.yaml` (repo root). Push to GitHub →
Render → **New + → Blueprint → pick repo** (or a manual Docker web service with
**Root Directory = `server`**, Dockerfile Path `./Dockerfile`, Health Check `/`,
Free plan). Render builds the image and gives a public URL; enter
`wss://<your-app>.onrender.com` in the app. See `server/README.md` for the full
walkthrough.

**Cold starts (handled).** The free instance sleeps after ~15 min idle and a
cold wake takes ~30–60s — longer than a WebSocket handshake will wait. The
client copes: `NetClient.connect()` first sends an HTTP GET to wake the host
(Render holds that request open until the instance is live), shows a
*"Waking the server…"* state, then connects with a 15s timeout and up to 5
retries. So the first connect after a sleep just takes a bit; it doesn't fail.

**Keep it warm (optional, free).** To avoid cold starts during play sessions,
have a free uptime pinger hit the URL every ~10 min:
[cron-job.org](https://cron-job.org) or [UptimeRobot](https://uptimerobot.com) →
monitor `https://<your-app>.onrender.com/` on a 10-minute interval. That keeps
the instance awake within the free tier's monthly hours.

### Still to grow (optional)

Server-side zone shrink + loot + match lifecycle (lobby → match → results), and
snapshot interpolation on the client for buttery motion at higher latency.

---

## 9. Publish to Google Play

1. **App icon & splash:** provide a 1024×1024 PNG logo (see §11) → `flutter pub add --dev flutter_launcher_icons` + config → `dart run flutter_launcher_icons`.
2. **App id / version:** already `com.zoneroyale.zone_royale`; bump `version:` in `pubspec.yaml` per release.
3. **Signing:** create an upload keystore (`keytool -genkey ...`), reference it in `android/app/build.gradle.kts` release signingConfig (replace the debug key).
4. **Build the bundle:** `flutter build appbundle --release` (re-enable R8 first, on a clean build box).
5. **Play Console:** create app → upload the `.aab` → fill store listing (title, short/full description, screenshots, feature graphic) → content rating → set up **AdMob** (rewarded video on death/retry, interstitial between matches) for revenue → roll out to internal testing, then production.

---

## 10. 🎨 Prompt for **Stitch AI** (premium UI redesign of all screens)

> Paste this into Google Stitch (or any UI-gen AI) to generate a premium visual
> design system + all app screens. Then I can re-skin `game_ui.dart` to match.

```
Design a premium mobile game UI kit for "ZONE ROYALE", a fast top-down battle-royale
shooter (think Free Fire / BGMI energy, but sleek and modern). Portrait, mobile-first,
dark theme.

BRAND & MOOD: Elite, tactical, high-energy but clean. Deep near-black background
(#05070C) with a subtle radial glow. Primary accent amber/gold (#FFB02E), secondary
danger red (#FF5A5F), info cyan (#37D0FF), success green (#52E06A). Rounded corners
(12–20px), soft glows, glassmorphism cards (blurred translucent panels with 1px
accent borders), bold condensed uppercase display type for headings, clean sans for
body. Micro-shadows and gradient buttons with a soft outer glow. Everything should
feel tactile and "worth tapping".

DESIGN THESE SCREENS:
1. SPLASH — animated logo reveal (a closing tactical ring / crosshair emblem) over a
   dark battlefield vignette, tagline "10 DROP IN. 1 WALKS OUT.", loading shimmer.
2. HOME / START — big ZONE ROYALE logo, a live 3D-ish operator preview card, player
   name + rank tag (colored pill) + coins, three match-mode cards (Skirmish 10 /
   Clash 25 / Warzone 50) with player counts, a map selector chip row, a glowing
   "DROP IN" primary button, and PROFILE · SHOP · DAILY MISSIONS secondary buttons.
3. PROFILE / LOADOUT — large operator preview, XP progress bar with rank + level,
   name field, swatch grids for outfit color / skin tone, chip rows for accessory /
   hero / starting weapon (locked items show a padlock), fire-mode toggle, control
   sliders, and a lifetime stats strip (matches / wins / win% / kills / best rank).
4. SHOP — sectioned list (SKINS / ACCESSORIES / WEAPONS / HEROES + Top-Form
   evolutions), each item a card with a preview dot, name, and a coin price button
   (or "OWNED"), coin balance in the header.
5. DAILY MISSIONS — 3 mission cards with progress bars and CLAIM buttons + reward
   (coins + XP), "refreshes daily" note.
6. IN-MATCH HUD — top: home button, ALIVE + KILLS pills, minimap with a red enemy
   radar; center: zone banner; bottom: health bar, grenade button (count), fire-mode
   button, a tappable weapon/ammo panel with reload progress; right: a circular hero
   SKILL button with a cooldown sweep; twin virtual joysticks; directional red damage
   arcs and a kill feed.
7. VICTORY / DEFEAT — huge "#1 WINNER WINNER" or "#3 ELIMINATED", kills + players,
   an XP/coins reward card with a LEVEL UP banner, PLAY AGAIN + HOME + SHARE buttons.

Deliver: a color/type/spacing style guide, reusable components (buttons, pills,
cards, sliders, progress bars, chips), and high-fidelity mockups of every screen
above in both light-idle and pressed/active states. Keep it consistent, premium,
and thumb-friendly for one-handed play.
```

---

## 11. 🖼️ Logo & splash — generation prompt + what's built

**Built in-app:** the game already draws its emblem in code (`EmblemPainter` in
`game_ui.dart`) — a closing tactical ring + crosshair + survivor dot — used on the
start screen. An animated splash screen is being added (logo scale/fade-in →
tagline → into the game).

**Prompt for an image AI (Midjourney / DALL·E / Stitch) to generate the raster
app icon + splash art:**

```
A premium mobile game APP ICON for "Zone Royale", a top-down battle royale.
Concept: a bold tactical emblem — a glowing amber-gold ring that is CLOSING inward
(the shrinking safe zone), with a sharp crosshair and a single white survivor dot at
the center, over a deep near-black background with a subtle red danger glow.
Flat, modern, high-contrast, iconic and readable at small sizes, rounded-square,
no text. Amber #FFB02E, danger red #FF5A5F, near-black #05070C. Clean vector style,
soft outer glow, premium AAA mobile-game quality. 1024x1024.

Also a matching SPLASH / hero image: the same emblem larger and centered over a
dark, atmospheric top-down battlefield vignette with faint tactical grid lines and
a soft gold light bloom, tagline space beneath reading "10 DROP IN. 1 WALKS OUT."
```

**Splash animation spec (implemented in Flutter):** on launch, the emblem scales up
from 0.7→1.0 with a fade-in over ~700ms, a gold ring sweeps around it, the title
"ZONE ROYALE" and tagline fade in, hold ~800ms, then cross-fade into the home
screen. Fully vector, no image files.

---

*Built iteratively, tested on device each step. Play a few rounds and tune from
there — balance numbers all live in `config.dart` and `royale_game.dart`.*
