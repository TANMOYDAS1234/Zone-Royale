name: zone_royale_server
description: Authoritative WebSocket multiplayer server for Zone Royale (prototype).
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.0.0
# No dependencies — pure dart:io. Run with:  dart run server/bin/server.dart
